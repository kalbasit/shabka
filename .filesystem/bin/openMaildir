#!/usr/bin/ruby
# $Id: openMaildir 1082 2008-05-28 06:36:45Z wael $

report = File.open("/home/wael/.mailReport.txt")
newmail = Array.new

report.each_line{ |line|
        if line.include? "new"
        print "Opening: "+line.split(" in ")[1]
        `urxvt -geometry 110x30+0+340 -e bash -c "mutt -f #{line.split(" in ")[1]}"`
        end
}
