nix

# install SWAY because nix-installed sway does not start. I believe it's missing a permission.
sway

# keybase to support the secrets of my dotfiles (private)
keybase
kbfs

# install fonts required by Sway and the terminal
# TODO: move these to Nix
ttf-croscore
powerline-fonts

# Manage network!
networkmanager

# Sound support
pulseaudio

# install chromium because it does not start when installed with Nix due to
# missing setuid on the binary.
# TODO: maybe add a postHook that uses sudo to setuid the binary?
chromium
