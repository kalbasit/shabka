[Unit]
Description=Locks the screen on suspend/hibernate

[Service]
ExecStart=/usr/bin/xss-lock --notifier=dim-screen --transfer-sleep-lock <%= ENV['MYFS'] %>/bin/transfer-sleep-lock-i3lock.sh
Restart=always

[Install]
WantedBy=default.target
