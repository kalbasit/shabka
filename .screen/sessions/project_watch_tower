# SESSION NAME=WatchTower Project

# Change to the project's folder
chdir $HOME/Projects/TechnoGate/watch_tower

# Source the config file
  source $HOME/.screen/config

# Overwrite default escape
  escape ^zz

# Overwrite default shell.
  shell -$SHELL

# Status bar
  caption always "%{=bu kb} %=< %?%-Lw%?%{kc}[:%n %t:]%{kb}%?%+Lw%? >%= "
  hardstatus alwayslastline "%{+b bw}[%H :: $USER]%{=b kw} %=%{=b bw}[%m.%d.%y %0C%a]"

  # Apps
  bind G screen -t 'Guard'      0 bundle exec guard
  bind P screen -t 'Pry'        2 bundle exec pry

# Default Buffers
#               <Title>         <Screen Number>     <Command>
  screen    -t  Guard           0                   bundle exec guard
  screen    -t  Log             1                   tail -f ~/.watch_tower/log/production.log
  screen    -t  Pry             2                   bundle exec pry
  screen    -t  Shell           3
  screen    -t  Shell           4
  screen    -t  Shell           5
  select 3
